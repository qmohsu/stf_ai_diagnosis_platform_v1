# ──────────────────────────────────────────────────────────────────
# OBD Agent  –  Environment Variables
# ──────────────────────────────────────────────────────────────────
# Copy to .env and edit as needed.
# Simulation mode (no hardware) is the default.

# -- Adapter / Vehicle ----------------------------------------------------
# Serial port for ELM327 adapter.
# Use "sim" for simulation (default), or a real port for live mode:
#   Linux USB:      /dev/ttyUSB0
#   Linux BT:       /dev/rfcomm0
#   macOS USB:      /dev/tty.usbserial-XXXX
#   macOS BT:       /dev/tty.OBDII-SPPDev
#   Windows USB:    COM3
OBD_PORT=sim
OBD_BAUDRATE=115200

# Pseudonymous vehicle identifier (NOT a raw 17-char VIN)
VEHICLE_ID=V-SIM-001

# -- Simulation -----------------------------------------------------------
# Scenario name from fixtures/simulation_scenarios.json
# Options: healthy, misfire, lean, overheat
OBD_SIM_SCENARIO=misfire

# -- Diagnostic API -------------------------------------------------------
DIAGNOSTIC_API_BASE_URL=http://127.0.0.1:8000
SNAPSHOT_INTERVAL_SECONDS=30

# -- Behaviour ------------------------------------------------------------
DRY_RUN=false
LOG_LEVEL=INFO
LOG_FORMAT=console
MAX_RETRY_ATTEMPTS=3
OFFLINE_BUFFER_MAX=100
