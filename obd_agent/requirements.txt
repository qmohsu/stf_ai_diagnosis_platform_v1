# OBD Agent -- analysis library + upload client dependencies.
# GPL-free since APP-53 cleanup (2026-06-09): the live ELM327
# acquisition loop (and its GPL-2.0 python-obd dependency) was
# removed; ingestion is end-of-trip log upload via jetson_uploader.

# HTTP client (jetson_uploader)
httpx==0.26.0

# Data validation
pydantic==2.5.3

# Time-series processing (Stage 1 pipeline)
pandas==2.1.4
numpy==1.26.3

# Anomaly detection (Stage 3 pipeline)
ruptures==1.1.9
scikit-learn==1.4.0

# Rule configuration (Stage 4 clue generation)
PyYAML==6.0.1

# --- Testing ---
pytest==7.4.4
pytest-asyncio==0.23.3
respx==0.21.1
